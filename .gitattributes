# Normalize all text files to LF in the repo (deterministic Linux/Vercel builds)
* text=auto eol=lf

# Treat binaries as binary (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
